version: '3.8'
services:
  cs340:
    build: .
    container_name: cs340_notebook
    ports:
      - "8888:8888"
    volumes:
      - .:/workspace
    environment:
      - JUPYTER_TOKEN=secret
    restart: unless-stopped
